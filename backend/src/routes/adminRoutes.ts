// import express from "express";
// import { isAdmin } from "../middlewares/checkAdmin.js"; 
// import { getPendingNGOs, approveNGO, rejectNGO, getNGODetails, getAllNGOs, totalFoodServed } from "../controllers/adminControllers.js";
// // import { authenticateUser } from "../middlewares/authMiddlware.js";

// const router = express.Router();

// // router.get("/ngos/pending", authenticateUser,isAdmin, getPendingNGOs);
// router.put("/ngo/approve/:id", isAdmin, approveNGO);
// router.put("/ngo/reject/:id", isAdmin, rejectNGO);
// router.get("/ngo/:id", isAdmin, getNGODetails);
// // router.get("/ngo",authenticateUser,isAdmin,getAllNGOs);

// router.get("/adminDashboard", isAdmin, totalFoodServed);


// export default router;
