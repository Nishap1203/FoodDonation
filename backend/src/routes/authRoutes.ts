// import express from "express";
// import { changePassword, confirmSignUp, forgetPassword, login, logout, signUp, resendCode } from "../controllers/authController.js";
// import { validateRequest } from "../middlewares/validateReq.js";
// import { changePasswordSchema, confirmSignupSchema, forgotPasswordSchema, loginSchema, resendCodeSchema, signUpSchema } from "../validations/authValidations.js";

// const router = express.Router();
// router.post("/signup",validateRequest(signUpSchema),signUp);

// router.post("/confirm-signup",validateRequest(confirmSignupSchema),confirmSignUp);

// router.post("/login", validateRequest(loginSchema),login);

// router.post("/logout",logout)

// router.post("/forget",validateRequest(forgotPasswordSchema),forgetPassword)

// router.post("/changepass",validateRequest(changePasswordSchema),changePassword)

// router.post("/resendCode",validateRequest(resendCodeSchema),resendCode)

// export default router;
