// import express from 'express';
// import { userProfile, updateProfile, getAllUsers, getUserById, createDonation, getMyDonations, getDonationById, cancelDonation } from '../controllers/userControllers.js';
// // import { authenticateUser } from '../middlewares/authMiddlware.js'; 

// const router = express.Router();

// // router.use(authenticateUser);

// router.get('/profile', userProfile);

// router.put('/profile', updateProfile);

// router.get('/users', getAllUsers);

// router.get('/users/:id', getUserById);

// router.post("/donations",createDonation);

// router.get("/donations",getMyDonations)

// router.get("/donations", getDonationById)

// router.post("/donations",cancelDonation)
// export default router;
